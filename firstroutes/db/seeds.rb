# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the rails db:seed command (or created alongside the database with db:setup).
#
# Examples:
#
#   movies = Movie.create([{ name: 'Star Wars' }, { name: 'Lord of the Rings' }])
#   Character.create(name: 'Luke', movie: movies.first)
require 'faker'

# 10.times do
#     User.create(username: Faker::Internet.username)
# end

# 50.times do
#     Artwork.create(title: Faker::Book.title, image_url: Faker::Internet.url, artist_id: User.all.sample.id)
# end

# 500.times do
#     ArtworkShare.create(viewer_id: User.all.sample.id, artwork_id: Artwork.all.sample.id)
# end

# 500.times do 
#     Comment.create(user_id: User.all.sample.id, artwork_id: Artwork.all.sample.id, body: Faker::Lorem.paragraph)
# end